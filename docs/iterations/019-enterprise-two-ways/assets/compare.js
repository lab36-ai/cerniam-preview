(()=>{const reduce=matchMedia('(prefers-reduced-motion: reduce)').matches,items=[...document.querySelectorAll('.reveal')];if(reduce){items.forEach(el=>el.classList.add('visible'));return}const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{rootMargin:'0px 0px -10% 0px',threshold:.08});items.forEach(el=>io.observe(el));window.__enterpriseTwoWays={get visible(){return items.filter(el=>el.classList.contains('visible')).length},get sections(){return items.length}}})();
